user             = {{user.stdout}}
password         = {{password.stdout}}
hosts            = {{hosts}}
dbname           = {{dbname}}
query            = SELECT goto FROM alias WHERE address='%s' AND active = '1'
#expansion_limit = 100
